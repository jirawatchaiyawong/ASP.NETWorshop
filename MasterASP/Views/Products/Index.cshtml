@model IEnumerable<MasterASP.Models.Product>

@{
    ViewBag.Title = "Index";
}

<style>

    .img-box {
        max-width:300px;
        max-height: 300px;
    }

    .img-box img {
        width: 100%;
    }
</style>




<div class="container-fluid" style="margin-top:2em;">
    <h2>All Product</h2>

    
    <button type="button" class="btn btn-outline-warning" style="">@Html.ActionLink("Create New", "Create")</button>
    
    @*<div class="bg-color">
        <table class="table">
            <tr>
                <th >
                    @Html.DisplayNameFor(model => model.ImageProduct)
                </th>

                <th>
                    @Html.DisplayNameFor(model => model.category.CategoryName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ProductName)
                </th>
                
                <th>
                    @Html.DisplayNameFor(model => model.UnitPrice)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UnitsInStock)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UnitsOnOrder)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Discontinued)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
        <tr>
            <td>
                <div class="img-box">
                <img src="~/@item.ImageProduct" />
                </div>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.category.CategoryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductName)
            </td>
            
            <td>
                @Html.DisplayFor(modelItem => item.UnitPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UnitsInStock)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UnitsOnOrder)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Discontinued)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ProductId }) |
                @Html.ActionLink("Details", "ProductView", new { id = item.ProductId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ProductId })
                @Html.ActionLink("Back to List", "Index")
            </td>
        </tr>
            }

        </table>*@


        <div class="container">
            <div class="row ">
                @foreach (var item in Model)
                {
                    <div class="col-md-4">
                        <div class="card border-secondary mb-3" style="max-width: 18rem;">
                            <div class="card-header">
                                <div class="img-box">
                                    <img src="~/@item.ImageProduct" />
                                </div>
                            </div>
                            <div class="card-body text-secondary">
                                <h5 class="card-title">@Html.DisplayFor(modelItem => item.ProductName)</h5>
                                <p class="card-text">หูฟังรุ่น : @Html.DisplayFor(modelItem => item.ProductName) </p>
                                <p class="card-text">ราคา : @Html.DisplayFor(modelItem => item.UnitPrice) บาท </p>

                                <button type="button" class="btn btn-outline-info">@Html.ActionLink("Details", "ProductView", new { id = item.ProductId })</button>
                                <button type="button" class="btn btn-outline-dark">@Html.ActionLink("Edit", "Edit", new { id = item.ProductId })</button>
                                <button type="button" class="btn btn-outline-danger">@Html.ActionLink("Delete", "Delete", new { id = item.ProductId })</button>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>

    </div>
</div>